import api from './common';

export const userApi = {
  // 유저의 사는 지역을 가져올 거임
  // login: async () => {
  //   return api.get('/users/login/success');
  // },
  // logout: async (data) => {
  //   return api.get('/users/logout', data);
  // },
};
